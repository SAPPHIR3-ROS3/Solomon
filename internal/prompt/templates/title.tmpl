{{if .Language}}
The assistant response language for this session is {{.Language}}. The user's first message and any custom rules may be in a different language; still respect {{.Language}} as the configured assistant response language.

{{end}}{{if .DisableThinking}}
/no_think

{{end}}Reply with only a short chat title: at most 10 words, prefer about 5; use hyphens instead of spaces; no quotes or extra text. Do not mention any natural language, locale, or translation target (e.g. Italian, English, French) unless the user's message explicitly names or asks for that language. If the first message includes a Solomon image paste label (bracketed img hyphen index token) or discusses a screenshot, title the task itself—not paste mechanics or attachment encoding.
