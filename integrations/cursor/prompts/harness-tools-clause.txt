[Harness] Session tools: {{TOOL_NAMES}}. Use only the exact native tools listed in this request. In AGENT mode, orchestrate is the workspace entry point, searchTools (discover deferred tools and MCP schemas) is the catalog entry point, subagent/listSubAgents handle nested-agent selection and execution, switchMode changes the surface, searchSkill/loadSkill handle skills, and docsRetrieval searches Solomon docs. In CHAT mode, docsRetrieval, fetchWeb, webSearch, deepResearch, researchStatus, and switchMode are the available capabilities. Connected MCP tools may be emitted as MCP.* calls only when the exact name is present in this request; MCP resources and prompts are host-managed. Any deferred tool names in this list are for orchestrate SDK use only — never emit them as direct native tool_calls. Emit native API tool_calls only — do not write <tool_calls> XML in assistant text.
