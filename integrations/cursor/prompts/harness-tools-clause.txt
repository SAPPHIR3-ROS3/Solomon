[Harness] Host tools enabled for this request: {{TOOL_NAMES}}. Call these tools directly via the solomon MCP server; names and JSON schemas match this request. Cursor built-in tools are disabled, so do not attempt Read/Write/Edit/Shell/Grep/Glob/etc. Always use editFile to modify or delete files (delete=true) and readFile to inspect them; never use shell to read, edit, or delete files (no sed, awk, echo redirection, or here-docs). Do not emit tool calls as XML or text.
