[Harness] Tool bridge: use normal Cursor built-in tools (Read, StrReplace, Write, Grep, Glob, Shell, Delete, SemanticSearch, Task, etc.). The Solomon host intercepts each call, maps it when possible, and executes on the real workspace. Host-backed capabilities for this session: {{TOOL_NAMES}}. Prefer Read for inspection and StrReplace/Write for edits over Shell when possible. If a call is rejected or unmappable, use Shell (with intent) as the default fallback when Shell is available. Do not emit tool calls as XML or text.
